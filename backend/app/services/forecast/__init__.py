"""Revenue forecasting: model ladder + conformal intervals.

Public entry point is `service.forecast_revenue`. Models live in `models.py`,
conformal interval machinery in `conformal.py`, data loading in `data.py`.
"""
