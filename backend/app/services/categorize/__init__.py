"""Content categorization: zero-shot video classification via sentence embeddings.

Public entry point is `service.categorize_texts`. The model (MiniLM embeddings +
per-category prototype cosine) lives in `model.py`.
"""
