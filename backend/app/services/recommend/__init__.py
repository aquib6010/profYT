"""Uplift recommendations: causal effect of content mix on revenue.

Public entry point is `service.recommend`. The doubly-robust (AIPW) estimator
lives in `model.py`, per-video feature loading in `data.py`.
"""
