# Production runtime dependencies (e.g. Render).
#
# Deliberately lean: the live API does NOT use torch / transformers /
# sentence-transformers (categorization is an offline script), nor prophet /
# econml / dowhy / shap. Excluding them keeps the build fast and fits a 512MB
# instance. LightGBM is also omitted — it's an optional forecast candidate that
# loses to ETS, and available_models() skips it gracefully when absent.

# Web / app
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
itsdangerous==2.2.0

# Data layer (Postgres in production)
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Auth
google-auth==2.37.0
google-auth-oauthlib==1.2.1
cryptography==44.0.0
httpx==0.28.1

# Scientific stack the live ML endpoints import
numpy==2.2.1
pandas==2.2.3
scipy==1.14.1
scikit-learn==1.6.0
statsmodels==0.14.4
ruptures==1.1.9
